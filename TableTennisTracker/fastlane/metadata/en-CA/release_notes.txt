The iPhone and iPad app is now fully native, so it starts faster and scrolls more smoothly. iPad gets a proper two-column layout — pick a session on the left, see its details on the right. Also fixed: settings rows that did nothing when tapped, and text that rendered in the wrong font.
